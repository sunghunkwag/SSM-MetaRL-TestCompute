# setup.py is deprecated
# See pyproject.toml for all build/metadata info, use pip only.
#
# This project uses modern Python packaging with pyproject.toml.
# All package metadata, dependencies, and versioning are managed in pyproject.toml.
# To install: pip install .
# To install in development mode: pip install -e .
